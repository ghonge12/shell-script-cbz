#!/bin/bash
#list of system defined varaibles =command {env}
echo ${SHELL}
echo ${HOME}
echo ${OSTYPE}
echo ${PATH}
echo ${$}  #process id
echo ${PIPD}  #parent process id
echo ${PWD}
echo ${HOSTNAME}
echo ${UID}
sleep 5
echo ${SECONDS}